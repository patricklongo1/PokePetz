export default async function Home() {
  /* const response = await api.get('/memories', {
    headers: {
      Authorization: `Bearer ${token}`,
    },
  }) */

  return (
    <div>
      <h1>Opa</h1>
    </div>
  )
}
